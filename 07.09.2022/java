package com.java.unit.junit_testing;

public class Largearr {public int get_maximum(int[]arr,int t) {
	int temp;
	for(int i=0;i<t;i++) {
		for (int j=i+1;j<t;j++)
		{if(arr[i]>arr[j])
		{temp=arr[i];
		arr[i]=arr[j];
		arr[j]=temp;
		}
		}
	}
return arr[t-1];
}}
